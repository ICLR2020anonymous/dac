import os

ROOT = "."

# path to save results
results_path = os.path.join(ROOT, 'results')

# path to save benchmark data
benchmarks_path = os.path.join(ROOT, 'benchmarks')

# path to save external data
datasets_path = os.path.join(ROOT, 'datasets')
